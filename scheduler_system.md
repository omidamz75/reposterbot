### [scheduler_system.md](file:///c%3A/Users/KPC/Desktop/reposter/docs/scheduler_system.md)
````markdown
# سیستم زمان‌بندی ربات تلگرام (Scheduler System)

## ویژگی‌های اصلی

### 1. مدیریت زمان ارسال
- تعیین ساعات کاری در روز (مثلاً 9 صبح تا 6 عصر)
- تعیین ساعات کاری در شب (مثلاً 8 شب تا 12 شب)
- تعیین فاصله زمانی بین ارسال‌ها (به دقیقه)
- امکان تعیین زمان پایان یا اجرای نامحدود

### 2. مدیریت نحوه ارسال
- حالت روز: پاک کردن تبلیغ قبلی قبل از ارسال تبلیغ جدید
- حالت شب: ارسال بدون پاک کردن تبلیغ قبلی
- چرخش خودکار بین تبلیغات
- امکان شروع از تبلیغ دلخواه

### 3. قابلیت‌های پیشرفته
- پاکسازی خودکار تمام تبلیغات در پایان سیکل
- لاگ‌گیری از تمام عملیات‌ها
- مدیریت خطاها و گزارش‌دهی
- امکان فعال/غیرفعال کردن زمان‌بندی

## ساختار دیتابیس

### جدول Schedule
```sql
CREATE TABLE schedules (
    id INTEGER PRIMARY KEY,
    owner_id INTEGER INDEX,
    mode ENUM('day', 'night'),
    day_start_time TIME,
    day_end_time TIME,
    night_start_time TIME,
    night_end_time TIME,
    interval_minutes INTEGER,
    clear_all_at_end BOOLEAN,
    start_from_ad INTEGER,
    last_ad_index INTEGER,
    has_end_time BOOLEAN,
    end_date DATETIME,
    is_active BOOLEAN,
    created_at DATETIME,
    updated_at DATETIME
);
```

### جدول ScheduleLog
```sql
CREATE TABLE schedule_logs (
    id INTEGER PRIMARY KEY,
    schedule_id INTEGER,
    advertisement_id INTEGER,
    channel_id STRING,
    message_id INTEGER,
    status STRING,
    error STRING,
    created_at DATETIME,
    FOREIGN KEY(schedule_id) REFERENCES schedules(id)
);
```

## منوی زمان‌بندی
```
⏰ مدیریت زمان‌بندی
├── ⚙️ تنظیم زمان‌بندی
├── 📋 نمایش تنظیمات
├── ▶️ شروع زمان‌بندی
├── ⏹ توقف زمان‌بندی
└── 🔙 بازگشت
```

## فرایند تنظیم زمان‌بندی
1. تعیین ساعت شروع روز (مثال: 09:00)
2. تعیین ساعت پایان روز (مثال: 18:00)
3. تعیین ساعت شروع شب (مثال: 20:00)
4. تعیین ساعت پایان شب (مثال: 23:59)
5. تعیین فاصله بین ارسال‌ها (به دقیقه)
6. تنظیم پاکسازی خودکار (بله/خیر)
7. تعیین زمان پایان (نامحدود/تاریخ مشخص)
8. انتخاب تبلیغ شروع

## سیستم چرخش تبلیغات
1. شروع از تبلیغ انتخاب شده
2. چرخش خودکار به تبلیغ بعدی
3. برگشت به اول لیست پس از اتمام
4. امکان تنظیم مجدد نقطه شروع

## سیستم لاگ‌گیری
- ثبت زمان و تاریخ هر ارسال
- ثبت وضعیت موفقیت/شکست
- ذخیره پیام خطا در صورت وجود
- نگهداری شناسه پیام‌های ارسال شده

## کلاس‌های اصلی

### 1. ScheduleExecutor
- مدیریت اجرای زمان‌بندی
- کنترل چرخه حیات عملیات
- مدیریت پیام‌های قبلی
- اجرای عملیات‌های همزمان

### 2. SchedulerService
- مدیریت تنظیمات زمان‌بندی
- کنترل وضعیت فعال/غیرفعال
- مدیریت چرخش تبلیغات
- ثبت لاگ‌ها

## مثال پیام‌های سیستم
```
✅ تنظیمات با موفقیت ذخیره شد!

🌞 ساعت روز: 09:00 تا 18:00
🌙 ساعت شب: 20:00 تا 23:59
⏱ فاصله ارسال: هر 30 دقیقه
🗑 پاکسازی خودکار: فعال
🔢 شروع از تبلیغ شماره: 2
⏰ زمان پایان: نامحدود
```
````